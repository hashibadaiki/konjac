{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main translator window.",
  "windows": ["main"],
  "comment": "Clipboard access goes through the read_clipboard/write_clipboard commands rather than the JS plugin, so the double-copy watcher can tell this app's own writes apart from the user's copies.",
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-minimize",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging",
    "core:event:allow-listen"
  ]
}
